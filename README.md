# generate-delivery-schedule
This is a RESTful API for an generating delivery schedule.

### Project Structure

```bash
├── src
├── .gitignore
├── jest.config.ts
├── Makefile
├── package.json
├── README.md
└── tsconfig.json
```

#### _Prerequisites_

- Ensure you have **NodeJS** installed by entering `node -v` on your terminal
  If you don't have **NodeJS** installed, go to the [NodeJS Website](http://nodejs.org), and follow the download instructions


### Using the Makefile to Testing

How to run all tests locally

1. `make install` - Installs dependencies.
2. `make dev` - Run the server
5. `make test` - Run unit test.

## _API Endpoints_

- Public API documentation of this project is available on [postman docs](https://documenter.getpostman.com/view/7775892/2s9Y5SXRQX)